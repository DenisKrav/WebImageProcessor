@* Замість аватарки стоїть заглушка, потім можна доробити, щоб кожний користувач мав своє власне фото профілю. 
    Також можна подумати про авторизацію за допомогою jwt токенів
*@

<nav class="header-navbar navbar-expand-md navbar navbar-with-menu navbar-without-dd-arrow fixed-top navbar-semi-light">
    <div class="navbar-wrapper">
        <div class="navbar-container content">
            <div class="collapse navbar-collapse show" id="navbar-mobile">
                <ul class="nav navbar-nav mr-auto float-left">
                </ul>
                <ul class="nav navbar-nav float-right">
                    <li class="dropdown dropdown-user nav-item">
                        <a class="dropdown-toggle nav-link dropdown-user-link" href="#" data-toggle="dropdown">
                            <span class="avatar">
                                <img src="~/theme-assets/images/portrait/small/avatar-default.png" alt="avatar"><i></i>
                            </span>
                        </a>
                        <div class="dropdown-menu dropdown-menu-right">
                            <div class="arrow_box_right">
                                @if (Context.Request.Cookies.ContainsKey("nickname") && !String.IsNullOrEmpty(Context.Request.Cookies["nickname"]))
                                {
                                    <a class="dropdown-item regBtn" href="/LoginLogoutReg/Logout"><i class="ft-user "></i><span class="user-name text-bold-700 ml-1">@Context.Request.Cookies["nickname"]</span></a>
                                }
                                else
                                {
                                    <a class="dropdown-item regBtn" href="/LoginLogoutReg/Login"><i class="ft-user"></i><span class="user-name text-bold-700 ml-1">Користувач</span></a>

                                }
                                <div class="dropdown-divider"></div>
                                @if (Context.Request.Cookies.ContainsKey("nickname") && !String.IsNullOrEmpty(Context.Request.Cookies["nickname"]))
                                {
                                    <a class="dropdown-item regBtn" href="/LoginLogoutReg/Logout"><i class="ft-power"></i>Вийти</a>
                                }
                                else
                                {
                                    <a class="dropdown-item regBtn" href="/LoginLogoutReg/Login"><i class="ft-power"></i>Вхід/Реєстрація</a>
                                }
                            </div>
                        </div>
                    </li>
                </ul>
            </div>
        </div>
    </div>
</nav>